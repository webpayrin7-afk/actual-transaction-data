# 집랩 UI 정책 2.0

2026-09-22 · 기존 정책을 대체하는 신규 설계안 · 코드 적용 전

## 1. 목적과 적용 범위

집랩은 가격·거래·주거비·지역·생활 정보를 비교하는 데이터 서비스다. 핵심 정보는 빠르게 파악되고, 반복 데이터는 조용하고 정확하게 비교되어야 한다. 동일한 정보 역할에는 동일한 스타일을 적용한다. 기존 정책 및 이전 대화의 초안과 충돌하면 본 문서를 새 기준으로 사용한다. 기존 문서를 실제 삭제하거나 사이트 코드를 변경한 것은 아니다.

전역 적용: 폰트, 정보 위계, 색, 여백, 표면, 버튼, 입력, 상태, 접근성. 페이지별 구성: 상세는 비교와 읽기, 검색은 입력과 결과, 시장 홈은 핵심 요약, 지도는 지도 탐색에 맞게 배치한다. 모든 페이지를 동일한 카드 배열로 만들지 않는다.

설계 결정: 지나치게 조밀한 금융 단말형과 큰 카드가 반복되는 홍보 페이지형 대신, 본문 15px·표 14px를 기반으로 대표값만 강조하는 정보 중심 UI를 채택한다. 모바일 가독성을 우선하고 데스크톱에서는 읽기 폭과 비교 배치를 조정한다.

## 2. 확인 근거와 한계

사용자 제공 IMG_8410~8417 모바일 캡처 및 lhr.life 잠실엘스 페이지의 실제 DOM/computed style을 확인했다. 실측 브라우저 뷰포트는 1363×936 CSS px. 모바일 CSS 실측은 하지 않았으며 모바일 배치는 캡처를 근거로 설계했다. 다른 페이지와 모든 탭을 점검한 것은 아니다.

| 항목 | 현재 페이지 실측 |
|---|---|
| 페이지 제목 | 30px / 34.5px, 700 |
| 섹션 제목 | 18px / 24.3px, 650 — 관리비도 동일 |
| 내부 소제목 | 16px / 21.6px, 650 |
| 관리비 요약 | 18px, 700 |
| 관리비 반복 금액 | 16px / 20px, 650 |
| 계산기 반복 금액 | 14px / 20px, 600 |
| 시세 요약 가격 | 16px / 20px, 650 |
| 일부 기준 문구 | 11px |
| 기간 버튼 | 높이 34px |
| 두 상세보기 링크 | 높이 42px, 같은 배경 rgb(7,150,154) |
| 정보 안내 버튼 | 일부 너비 13px |
| 섹션 카드 | padding 20px, radius 16px |
| 페이지 섹션 gap | 40px |
| 카드 폭 / 상단 정보 폭 | 약 1268px / 896px |
| 본문 지정 서체 | Noto Sans KR 계열 |
| 제목 지정 서체 | Outfit 및 시스템 fallback 계열 |

지정 font-family는 실제 한글 글리프의 렌더링 서체를 보장하지 않는다. 다만 제목과 본문에 다른 폰트 스택이 지정된 것은 확인했다. 관리비의 문제는 제목 크기가 아니라 반복 금액의 역할별 규격 차이와 강도다. 이전 캡처 분석에서 두 상세보기 버튼 색상이 다르다고 했으나 현재 페이지 실측에서는 같아 정정한다.

관리비 산정근거에는 기준월 2026년 7월, 선택 면적, 산식, 출처, 여름 2개월 평균이 이미 존재한다. 새 정책은 이 정보를 새로 만들기보다 핵심 조건을 요약 옆에 노출한다.

학교 탭 선택 후 “This page couldn’t load” 화면이 관찰됐고 한 번 새로고침 후 본문이 복구됐다. 학교 기능 결함 또는 특정 원인으로 단정하지 않는다. 별도 재현 점검 대상이다. 사용자 확인에 따라 네이버 지도 미표시는 인증 문제이므로 이번 UI 진단·개선 범위에서 제외한다.

## 3. 타이포그래피

본문과 제목 모두 Noto Sans KR → system-ui → sans-serif 단일 스택. 로고는 별도 그래픽 자산으로 유지한다. 기존 제목용 Outfit 스택은 UI 텍스트에서 제외한다. 폰트 굵기는 400/500/600/700 네 가지. 일반 자간 0, 제목만 -0.02em. 숫자는 tabular-nums, 금액/단위는 필요한 범위에서 한 묶음으로 유지한다.

표는 모바일 / PC의 크기·행간이며 단위는 CSS px. 구현은 rem을 사용하고 사용자 글자 확대를 허용한다. PC 기준은 1024px 이상, 768~1023px는 모바일 크기를 유지한다.

| 역할 | 모바일 | PC | 굵기 | 적용 |
|---|---|---|---|---|
| page-title | 24/32 | 28/36 | 700 | 단지명·학교명·페이지명 |
| section-title | 18/26 | 20/28 | 700 | 거래내역·관리비·지역비교 |
| subsection-title | 16/24 | 16/24 | 600 | 관리비 구성·가격 비교 |
| hero-value | 28/36 | 32/40 | 700 | 페이지 최상단의 단일 대표 지표, 선택적 |
| summary-value | 20/28 | 24/32 | 600 | 최근 가격·관리비 요약·계산 결과 |
| compact-value | 18/26 | 18/26 | 600 | 여러 개가 반복되는 시설 수 |
| list-title | 16/24 | 16/24 | 600 | 거래금액·단지/학교 목록명 |
| data-value | 15/22 | 15/22 | 500 | 일반 값, 금액 비교만 600 |
| body | 15/24 | 15/24 | 400 | 설명문 |
| label/table/control | 14/20 | 14/20 | 400/500 | 라벨·표·버튼, 활성 600 |
| caption | 13/20 | 13/20 | 400 | 출처·날짜·조건 |
| badge (micro) | 12/16 | 12/16 | 500/600 | 배지형 요소에 한정 — 요약 라벨 태그(`LabTag`), 신고가 배지, 순위 원, 탭 카운트, 차트 축·미니차트 눈금, 달력 칸 숫자·요일 |
| input | 16/24 | 16/24 | 400 | 입력값 |

큰 수치를 모든 섹션에 배치하지 않는다. 반복 표의 값은 15px, 요약은 20px, 페이지의 단일 대표값만 28px다. 현재 단지 상세는 시세 요약의 매매/전세를 20px로 쓰고 별도의 28px 가격을 중복 추가하지 않는다. 비교값은 동일 규격이며 선택 대상을 강조할 때 크기를 바꾸지 않는다.

라벨을 작고 흐리게 줄이는 방법으로 위계를 만들지 않는다. 일반 콘텐츠 최소 14px, 보조 설명 최소 13px. 12px는 테두리·배경이 있는 배지형 요소와 축에만 쓰고, 문장·날짜·조건 같은 보조 텍스트는 13px(caption, `detail-meta`)로 쓴다. 12px 미만(10·11px)은 사용하지 않는다. 사용자 글자 확대 시 고정 높이를 풀어 내용이 늘어날 수 있어야 한다.

## 4. 색상 체계

| 토큰 | 값 | 용도 |
|---|---|---|
| text/primary | #0F172A | 제목·가격·주요 값 |
| text/body | #334155 | 본문 |
| text/secondary | #64748B | 라벨·날짜·출처 |
| brand/primary | #0F766E | 활성·링크·주요 동작 |
| brand/hover | #115E59 | hover/pressed |
| brand/subtle | #F0FDFA | 선택 배경 |
| canvas | #F8FAFC | 페이지 |
| surface | #FFFFFF | 본문 카드·입력 |
| surface/subtle | #F1F5F9 | 비선택 보조 배경 |
| border/subtle | #E2E8F0 | 카드·행 구분 |
| border/control | #64748B | 테두리가 식별에 필요한 입력/컨트롤 |
| change/up | #B91C1C | 가격 상승 |
| change/down | #1D4ED8 | 가격 하락 |
| warning/text | #92400E | 주의 문구 |
| error/text | #B91C1C | 오류 문구, 아이콘/텍스트 병기 |
| success/text | #166534 | 완료 상태 |

로고 색을 변경하는 작업은 본 정책에 포함하지 않는다. 사이트 UI의 행동색은 위의 한 가지 청록으로 통일한다. 일반 금액은 primary, 변화량에만 상승/하락색. “최고가 대비 -10.1%”는 down으로 표시한다. 가격 하락이 사용자에게 나쁜 일이라는 의미를 넣지 않는다. 청록은 클릭 가능성뿐 아니라 현재 비교 대상에도 사용하므로 비교 대상은 ‘선택 단지’ 등 구조적으로 식별한다. 링크는 밑줄·화살표·버튼 형태로 동작을 구분한다.

차트 계열: 매매 #087F83, 전세 #C2410C(가격 잉크와 동일), 거래량 #0F766E(보조 막대는 옅게 처리). 차트 계열색은 변화량 색상과 별도 토큰이다. 색 외에 범례명·선/막대 형태를 함께 사용한다.

상권 범주: 식음 #C2410C, 쇼핑 #0369A1, 생활서비스 #475569, 교육 #7C3AED, 여가체육 #15803D, 의료건강 #BE185D. 지도·아이콘·차트에 같은 매핑을 사용한다. pastel 배경은 색상 토큰의 연한 변형으로 지정하고 텍스트는 본문색을 유지한다. 순위 막대는 비교 목적일 때 단색을 기본으로 하며 범주 구분이 필요할 때만 범주색을 사용한다.

구현 검증 목표: 일반 텍스트 대비 4.5:1, 큰 텍스트 및 필수 컨트롤/그래픽 3:1. 장식 구분선과 상호작용 경계선을 같은 토큰으로 취급하지 않는다. 실제 배경·투명도가 반영된 조합을 측정한다.

## 5. 레이아웃·여백·표면

전역 여백 단위: 4,8,12,16,24,32,48. 별도 규격으로 카드 padding 20은 쓰지 않는다.

| 위치 | 모바일 <768 | 태블릿 768~1023 | PC ≥1024 |
|---|---|---|---|
| 페이지 좌우 | 16 | 24 | 32 |
| 상세 본문 최대 폭 | 가용 폭 | 가용 폭 | 1120 |
| 독립 카드 사이 | 24 | 24 | 32 |
| 카드 내부 | 16 | 24 | 24 |
| 페이지 제목 → 본문 묶음 | 24 | 24 | 32 |
| 제목 → 기준/설명 | 4 | 4 | 4 |
| 제목 묶음 → 본문 | 16 | 16 | 24 |
| 내부 묶음 사이 | 24 | 24 | 24 |
| 소제목 → 내용 | 12 | 12 | 12 |
| 라벨 → 값 | 4~8 | 4~8 | 4~8 |
| 컨트롤 사이 | 8 | 8 | 8 |
| 본문 → 하단 동작 | 16 | 16 | 16 |

최대 폭은 바깥 상세 컨테이너 기준이다. 상단 단지정보·차트·카드의 좌우 기준선을 동일하게 맞춘다. PC에서 카드 외곽이 넓더라도 좁은 폼과 라벨/값은 내부 최대 640px 범위에 두어 라벨과 값 사이가 수백 px 비지 않게 한다. 관리비·계산기 두 정보 묶음은 여유 폭에서 내부 2열로 배치할 수 있으나 비교 순서는 유지한다. 시장 대시보드는 최대1280, 지도는 전체 폭 등 페이지 유형별 예외를 명시한다.

카드 radius12, 버튼/입력8, 팝오버12, 바텀시트 상단20, 배지는 pill. 기본 카드에 그림자를 넣지 않는다. 경계는 1px border/subtle. 떠 있는 팝오버만 그림자를 사용한다. 바깥 카드 안의 목록을 다시 둥근 카드로 감싸지 않고 행 구분선을 우선한다. 주변 공급처럼 독립 클릭 대상인 항목 카드는 예외다.

일반 정보행 min-height44, 비교표 행44, 두 줄 거래행 min-height64. 내용이 길면 높이가 늘어난다. 셀 좌우12, 상하10~12를 적용하되 최소 높이와 중복으로 과도한 여백을 만들지 않는다. 제목을 제외한 단순 라벨/값은 좌측 라벨·우측 수치. 숫자 열 우측 정렬, 날짜 표기는 일관되게 한다.

## 6. 공통 컨트롤·탐색

기본 버튼 min-height44, 입력48, 주요 입력 검색48. 모든 터치 영역44×44. 아이콘은16 또는20이고 시각 크기와 터치 영역을 구분한다. 정보 아이콘13px 크기를 터치 영역으로 그대로 쓰지 않는다. focus-visible은 2px brand outline, offset2. disabled는 흐리게 하되 상태를 속성과 문구로도 구분한다.

Primary: 검색 실행·저장 등 우선 동작, 청록 배경/흰 글자. Secondary: 상세보기·비교 진입, 흰 배경/청록 글자/컨트롤 경계. Tertiary: 근거 보기·초기화, 텍스트형. 버튼 폰트14/20·500, primary/활성600. 페이지에 filled 탐색 버튼을 반복하지 않는다. 거래 내역 자세히 보기와 지역 순위 보기는 동일 Secondary.

페이지 이동/섹션 이동은 밑줄 탭, 한 영역의 보기 모드 선택은 segmented control, 면적/기간 등 조건은 필터로 구분한다. 각 유형 안에서 규격을 통일한다. 선택 시 글자 크기나 버튼 폭이 바뀌지 않게 한다. 모바일에서 제목 옆 필터가 좁아지면 필터를 다음 행으로 내린다. 전체 메뉴를 축소해 한 줄에 강제로 넣지 않는다.

페이지 상단은 PageHeader로 뒤로가기44, 제목, 보조설명 규격을 통일한다. 스크롤 후 압축 헤더가 필요한 경우 기존 헤더와 동시에 중복 점유하지 않고 한 레벨로 전환한다. 단지명16/24·600, 면적 선택14/20. 섹션 이동바와 겹칠 경우 합계 높이와 스크롤 오프셋을 공통 관리한다. 애니메이션120~180ms, reduced-motion에서는 제거한다.

모달/바텀시트: 제목18/26·700, 본문15/24, 옵션44 이상, 선택표시와 텍스트 동시 제공. 초점 이동·복귀, Escape 닫기, 배경 스크롤 제어, 하단 safe-area 대응. 예외적인 소형 폰트를 사용하지 않는다.

## 7. 단지 상세 역할 매핑

| 영역 | 신규 적용 |
|---|---|
| 단지 기본정보 | 제목24/28. 주소·입주·세대14, 추가 건축조건13. 면적 선택48 |
| 시세 요약 | 모바일 2×2, PC 4열. 가격20/24, 라벨14, 날짜13. 거래량 숫자도 동일 요약 위계, 거래유형은 라벨로 분리 |
| 시세 차트 | 모바일 플롯220, PC280 목표(범례/축/조작부 별도). 축12, 범례13, 툴팁14. 이중축에 억/건 명시 |
| 거래내역 | 날짜14, 보조13, 가격16·600·primary. 연도 경계는 연도 표시. 내부 중첩 카드 제거 |
| 계산기 | 입력16, 라벨14, 합계20/24·600, 세부값15·600. 예상치·산정조건13 |
| 지역 비교 | 소제목16, 순위18, 가격비교 수치15. 단위는 차트 제목/헤더에 공통 표기 가능 |
| 주변 단지 비교 | 이름14·600, 조건13, 가격15·600, 기타값14. 선택 단지의 열만 옅은 배경. 좁으면 표 내부만 가로 스크롤 |
| 주변 생활 | 탭44, 기준13 한 번. 지도260 모바일/360 PC. 시설 수18, 시설명13, 아이콘20/박스32, 3열 기본 |
| 상권 구성 | 개별 막대+비율14를 기본. 같은 정보를 반복하는 누적 막대 제거. 막대에 같은 스케일 적용 |
| 주변 공급 | 이름16·600, 보조13, 입주일14·500, 링크14. 배지는 줄바꿈 허용 |
| 관리비 | 최근 예상액20/24·600. 기준월·선택 면적13을 요약 아래 노출. 평균/구성 라벨14, 금액15·600. 내부 제목16 |

관리비의 “최근 12개월 평균”은 실제 데이터가 12개월 미만이면 “최근 N개월 평균”으로 표기한다. 현재 데이터가 실제로 몇 개월인지 확인하기 전 N을 임의로 넣지 않는다. 여름 2개월 평균처럼 비교 해석에 중요한 범위는 근거 패널 안에만 숨기지 않는다. 반올림 때문에 합계가 맞지 않을 수 있으면 안내를 제공하되 실제 산식 확인 후 문구를 결정한다.

차트 툴팁은 화면 가장자리에서 위치 전환, max-width min(280px, viewport-32px). 수치가 잘리지 않게 하고 hover 전용으로 만들지 않는다. 슬라이더 시각 핸들은16~20, 터치 영역44. 선/범례 이름으로 구분하며 선택 데이터의 텍스트 접근 경로를 제공한다. 전체 기간에 점을 과도하게 표시하지 않는다.

## 8. 로딩·빈 상태·오류·출처

로딩은 기존 공간에 스켈레톤/진행 문구, 자료 없음은 ‘자료 없음’과 이유/다른 조건, 실패는 오류와 재시도. 자료 없음과 실제0을 구분한다. 부가 섹션 실패가 상세 전체를 대체하지 않도록 섹션별 오류 경계를 둔다. 정상 데이터가 있는 다른 섹션은 유지한다.

출처/날짜/범위는13px·secondary. 설명을 지나치게 흐리게 처리하지 않는다. 섹션 공통 조건은 한 번, 개별 값이 다른 조건을 가질 때 해당 값 옆에 별도 표기한다.

하단 고정 요소는 safe-area를 반영하고 마지막 콘텐츠가 가려지지 않게 한다. 스크린샷의 Safari 주소창 및 운영체제 영역을 사이트 여백으로 계산하지 않는다.

## 9. 구현과 수용 기준

단일 토큰 파일과 공통 역할 컴포넌트를 사용한다: PageHeader, Section, SectionHeader, Subsection, Metric, DataRow, DataTable, Control, SourceNote, AsyncState. 실제 이름은 저장소 구조를 따른다. 페이지별 임의 font-size/색을 허용하지 않고 의미가 다른 예외만 문서화한다. 기존 정책 클래스는 사용처를 매핑·치환한 후 제거한다. 레거시와 신규 토큰이 같은 역할로 병존하지 않게 한다.

현 단지상세 작업 브랜치에서 진행. 신규 정책 정의를 먼저 문서화하고 잠실엘스, 긴 단지명, 자료 없는 단지로 샘플 적용. DB 적재·재스캔·수집/집계 로직은 변경하지 않는다. API가 제공하는 기간 정보로 표현을 바꾸는 경우 데이터 계약을 먼저 확인한다. 네이버 지도 인증 문제는 조사·수정하지 않는다. 학교 탭 전환 후 로드 오류는 원인이 미확정된 별도 관찰 사항으로 기록한다. 확정된 로고를 임의 변경하지 않는다.

검증 폭360/390/430/768/1280, 글자200% 확대, 키보드 탐색, 포커스, 긴 이름/가격, 모든 상태, 차트 경계, 탭 활성, 44px 터치, 색 대비. 글자가 잘리거나 페이지 전체가 가로로 넘치면 미완료. 표 내부 가로 스크롤은 허용한다. DOM/computed style로 역할별 값 일치 확인. 스크린샷·화면 녹화 생성 금지. 필요한 기존 타입 검사/린트/빌드만 실행하고 무관한 테스트 확대를 하지 않는다.

적용 순서: 토큰 및 기준 컴포넌트 → 관리비/계산기/거래행 → 시세/비교/생활 → 다른 페이지의 동일 역할 확장. 전역 컴포넌트 변경 시 주요 페이지 영향 확인. Production 배포와 병합은 별도 요청 전까지 하지 않는다.

완료 보고는 복사 가능한 단일 코드블록: 적용 범위, 구 정책 제거 범위, 최종 토큰, 검증 결과, 미검증/잔여 오류. 마지막 줄에 Preview 링크만 코드블록 밖 표시. 실제 검증하지 않은 항목은 완료라고 보고하지 않는다.


## 10. 모바일 우선 적용 기준 — 사용자 피드백 반영

PC 실측만으로 모바일 완료 판정을 내리지 않는다. 현재 근거는 제공된 iPhone 캡처 8장에 대한 시각 검토다. 360/390/430 CSS px의 실제 브라우저 렌더링, 모바일 computed style, 터치 동작은 미검증이다. 현재 연결 브라우저의 공개 조작 API에는 뷰포트 변경 기능이 없어 이 세 크기의 실측을 완료했다고 주장하지 않는다. 캡처 픽셀을 CSS px로 단순 환산하지 않는다.

### 캡처에서 확인한 현상

- 시세 4열 요약에서 최고가 대비 설명이 여러 줄로 꺾인다. 제목·기간 버튼이 같은 줄을 차지한다.
- 거래행 왼쪽에 날짜와 층/면적/거래유형, 오른쪽에 금액이 있다. 2줄 구조는 유지할 가치가 있다.
- 계산기 입력값과 한글 환산 금액이 같은 입력 영역의 좌우를 점유한다.
- 가격 비교는 지역명·막대·단위 포함 금액이 한 행에 있어 좁은 폭에서 경쟁한다.
- 주변 단지 비교는 라벨 1열+단지 3열이다. 열을 유지하려고 글자를 줄이는 방식은 피해야 한다.
- 생활시설 3×3 배열과 여러 차트가 누적되어 스크롤 길이가 길다.
- 주변 공급의 유형·동·세대수·면적 배지가 한 줄에 몰린다.
- 관리비는 반복 숫자가 강하다. 평균 기준과 구성 항목의 위계 분리가 필요하다.

### 모바일 배치 결정

| 요소 | 모바일 정책 |
|---|---|
| 페이지/카드 | 바깥16, 안쪽16. 안쪽 목록에 추가 외곽 padding을 중복하지 않는다. 390px 기준 카드 본문은 테두리 제외 약326px 확보 |
| 시세 헤더 | 제목 행과 기간 필터 행 분리. 기간 필터4개 동일 폭, 높이44, 간격8 |
| 시세 요약 | 2×2, 셀 간격12. 셀의 라벨14/값20/날짜13. 값 줄바꿈을 위해 글자를 축소하지 않음 |
| 전세가율/갭 | 두 항목을 줄바꿈 가능한 묶음으로 처리. 360px에서는 2줄 허용 |
| 거래 헤더 | 제목/면적 설명 다음에 매매·전세·월세3개 탭. 탭 높이44 |
| 거래행 | 날짜14, 설명13, 금액16. 좌측 min-width0, 우측 금액 nowrap. 설명은 자연스러운 최대2줄 배치, 행 높이는 증가 허용 |
| 계산 입력 | 숫자 입력48 높이. 한글 환산 금액은 입력 아래13px로 이동해 좌우 경쟁 제거 |
| 계산 조건 | 라벨 위·선택창 아래의 세로 배치. 선택창 전체 폭48 높이 |
| 계산 결과 | 합계20px. 긴 합계가 맞지 않으면 라벨 다음 줄에 값, 값 자체를 축소하지 않음 |
| 지역 가격 비교 | 첫 줄 지역명+우측 수치, 다음 줄 막대. 만원/평 단위는 공통 헤더에 표시 |
| 주변 단지 표 | 첫 라벨열72px, 단지열 최소104px. 필요 시 표 내부 가로스크롤, 첫 라벨열 고정. 끝 열을 접근할 수 있다는 안내 제공 |
| 주변 생활 탭 | 4개 동일 폭, 글자14, 높이44. 지도 인증 상태는 이번 점검에서 제외 |
| 시설 그리드 | 3열, 수치18, 라벨13, 아이콘20/배경32, 행 간격20. 큰 글자 설정에서는2열로 전환 |
| 주변 공급 | 이름 → 보조정보 → 면적 배지 → 입주일/링크의 묶음. 배지는 wrap, 긴 이름은 줄바꿈 |
| 관리비 | 요약20px, 일반 금액15px, 라벨14px. 기간/조건13px. 두 금액 열을 추가하지 않고 라벨-값 구조 유지 |
| 보조 출처 | 제목 우측에 무리하게 끼우지 않고 제목 아래13px로 표시 |
| 압축 헤더 | 뒤로가기44+단지명16. 이름과 면적 선택이 겹치면 면적 선택을 별도 행으로 배치. 고정바 누적 점유 최소화 |

버튼 터치 영역 확대는 보이지 않는 영역을 서로 겹쳐 구현하지 않는다. 겹침 없이44px 영역을 확보하며 필요하면 줄 배치를 바꾼다. 단순 정보행에는 일괄44px 최소 높이를 강요하지 않고 행간과10~12px 상하 여백을 사용한다. 클릭 가능한 행에44px를 적용한다. 이 규칙은 앞의 일반 정보행 min-height44 기준을 대체한다.

### 모바일 완료 조건

360/390/430px 각각에서 실제 렌더링과 computed style을 확인할 것. 390px를 기본 설계 폭으로, 360px를 최소 폭으로, 430px를 넓은 모바일로 검사한다. 금액·단위 잘림, 제목/탭 충돌, 툴팁 화면 이탈, 전체 페이지 가로스크롤이 없어야 한다. iOS Safari의 입력 포커스와 하단 안전영역, Android Chrome의 줄바꿈은 별도로 점검한다. 모바일 실기기에서 확인하지 못했다면 데스크톱 에뮬레이션 결과와 구분해서 보고한다.

## 11. 랩시리즈 공통 선택 UI (segmented family)

탭·분류·기간 필터는 **하나의 segmented control 계열**을 쓴다. 위계는 형태가 아니라 **크기·배치·콘텐츠 범위**로만 나눈다.

기존 1차 독립 버튼형·폴더형(콘텐츠 연결 밑줄형)·진한 청록 채움형·단순 밑줄형은 폐기한다.

공통 문법:
- 흰 셸 배경 `#FFFFFF` + 얇은 경계 `#E2E8F0`, 셸 radius 12px, 트랙 padding 4px
- 선택 면 `#EEFAFA`(teal-50), radius 8px (= 셸 12 − 패딩 4, 동심원) — 안쪽 테두리 없음
- 선택 글자 `#087F83`(teal-700), 비선택 글자 `#64748B`
- 선택 면과 바깥 테두리 사이 여백은 상·하·좌(첫 항목)·우(마지막 항목)가 동일
- 동일 폭 기본, 활성 시 글자 크기·항목 폭·전체 높이 불변
- 선택 면 이동 160ms, `prefers-reduced-motion` 시 제거
- 금지: 그라데이션, 진한 청록 전체 채움, 밑줄형, 회색 트랙+흰 선택 면

### 1차 — primary (48px)

- 적용: 매수비용 / 보유세 / 대출 등 주요 모드
- 높이 48px, 글자 16/24, 선택 600 / 비선택 500, 콘텐츠 영역 전체 폭
- 제목·설명 아래 16px, 선택기 아래 첫 콘텐츠까지 24px
- 카드 좌우 콘텐츠 padding 안에 배치 (full-bleed·폴더형 금지)

### 2차 — secondary (40px)

- 적용: 매매/전월세, 가격 수준/변동률, 주변 생활 내부 분류 등
- 높이 40px, 글자 14/20, 선택 600 / 비선택 500
- 섹션 전체 또는 레이아웃에 맞는 적정 폭

### 3차 — compact (30px visual)

- 적용: 시세 1년/3년/5년/전체 등 기간·조건 preset
- 보이는 높이 30px, 글자 13/20, 선택 600 / 비선택 500, 내용 너비
- 시세 추이 제목 오른쪽 우선, 충돌 시 다음 행 우측 (글자 축소 금지)
- 실제 터치 영역 항목별 44×44 이상, 숨은 영역 겹침 금지
- preset과 슬라이더 양방향 연동. 임의 기간이 preset과 불일치하면 활성 표시 해제

### 접근성

- 색과 선택 면 형태로 상태 표시
- 콘텐츠 패널 전환: tablist / tab / tabpanel (+ aria-controls)
- 조건 선택: radiogroup / radio
- 방향키·Enter/Space, focus-visible 2px 청록 outline offset 2px
- 글자 200% 확대 시 높이 증가 허용. 모바일 묶음 약 5개 이하 우선
- 360px에서 넘치면 해당 컨트롤만 가로 스크롤 또는 기존 드롭다운 (페이지 가로 스크롤 금지)

### 구현

- `LabTabs` variant=`primary` | `secondary` | `compact` (`src/components/ui/LabTabs.tsx`)
- 공통 트랙 + `.lab-tabs__thumb` 슬라이딩 선택 면 (`src/app/globals.css`)
- 색·radius·그림자는 `--lab-*` / `--lab-tabs-*` 토큰에서 관리 (페이지별 하드코딩 금지)
  - 선택 면 `--lab-tabs-thumb-bg` `#EEFAFA`, 선택 글자 `--lab-tabs-active-ink` `#087F83`, 트랙 패딩 `--lab-tabs-track-pad` 4px, thumb radius = shell − pad (글로벌 `--lab-teal-*`와 구분)
- SSR/hydration 시 첫 측정 후에만 thumb transition 활성화
## 12. 랩시리즈 공통 패턴 — 「시장 현황」 기준 (2026-09-23)

기준 페이지: `/region/[slug]?tab=stats` (지역 조회 › 시장 현황, 구 단위). 이 장의 규칙과 컴포넌트는 앞 장과 충돌하면 이 장이 우선한다. 다른 페이지(단지 상세, 동 단위 지역, 검색·홈)는 새로 만들거나 손볼 때 이 장을 따른다.

### 12.1 페이지 구성 원칙

- **섹션 = 주제 묶음.** 한 섹션 카드에는 한 주제만 둔다. 섹션 순서는 사용자의 질문 순서를 따른다.
  시장 현황 예: 시세(평당가 → 동네별 → 전세) → 거래 흐름(거래 동향 → 신고가) → 단지 찾기(랭킹 → 예산) → 공급·원자료(입주 예정 → 거래 내역).
- **같은 비교는 한 곳에만.** 동일 지표(예: 직전 거래 대비 오름/내림)를 여러 섹션에 반복하지 않는다. 추세는 한 곳, 특정 월 드릴다운은 해당 선택 컨트롤 옆에 둔다.
- **하위 영역은 섹션당 2~3개 이하.** 숫자 두세 개뿐인 블록은 소제목을 따로 달지 않고 인접 하위 영역의 지표 박스로 흡수한다(예: 거래 신호 → 시장 온도 박스).
- **구분선은 하위 영역 경계에만.** 하위 영역 안의 항목 구분에 같은 선을 쓰지 않는다(항목은 박스·간격으로 구분). `LAB_SUBSECTION_RULE`.
- **진행 중 데이터 왜곡 방지.** 신고 지연(계약 후 30일)으로 이번 달이 비는 지표는 노출하지 않거나 "진행 중"을 명시한다. 월초 사용자가 늘 왜곡된 값을 보게 하지 않는다.
- **배경 틴트 금지.** 섹션 안 KPI·요약에 연한 청록 배경을 깔지 않는다. 강조는 글자 크기·굵기·테두리로 한다(청록 배경은 선택 상태와 강조 칩에만).

### 12.2 섹션 · 하위 영역 헤더

**페이지 헤더(`PageHeader` + `leading`)**: 뒤로가기(44px)는 제목 첫 줄의 세로 중앙에 맞춘다. 제목 · 보조(서울특별시 등 `titleSuffix`)는 뒤로가기 오른쪽 한 줄. 요약 속성(지역: 단지 수 · 세대 · 평균 연식 · 주력 평형)은 문장형 소개 줄 대신 **라벨 행**(`LabTag size="md"`, 12px · 26px 높이 · 간격 4)으로 제목 아래 10px, 본문과 같은 왼쪽 기준선에 둔다. 360px에서 한 줄이 되도록 값을 다듬는다(평균 연식은 정수). 여러 줄 설명이 필요한 단지 상세는 소개 줄을 제목 기준선에 맞춰 4px 아래에 두고 의미 단위로 줄을 나눈다. 헤더 → 첫 탭·본문은 shell gap(모바일 20 / PC 24).

| 역할 | 컴포넌트 | 규격 |
|---|---|---|
| 섹션 카드 | `LabSection` / `LAB_SECTION_SURFACE` | `lab-card detail-card`, 내부 gap 12 |
| 섹션 제목 줄 | `LabSectionHeader` | 제목(section-title) + (i)팁 왼쪽 · 기준/범위 메타(13px secondary) 오른쪽, 좁으면 메타가 다음 줄 |
| 하위 영역 제목 | `LabSubsectionHeader` (h3/h4) | 16/24·600 navy + (i)팁, 메타 오른쪽 |
| 하위 영역 안 소제목 | `h5.detail-data-value-emphasis` | 15/22·600 navy (예: "어디서 거래됐나") |

- **메타는 기준을 말한다**: "계약일 기준", "공급면적 기준", "최근 3개월 · 매매 598건", "2026.08 기준 · 최근 3개월 실거래".
- **(i)팁은 의미만.** 가중치·산식·표본 기준 같은 내부 노하우는 쓰지 않는다. 사용자가 해석에 필요한 정의(예: "고점 −10%: 종전 최고가보다 10% 이상 낮은 거래")만 쓴다.
- **회색은 라벨에만.** 단지명·동 이름·목록 항목 이름은 회색 라벨이 아니라 navy 데이터 글꼴(`detail-data-value`)을 쓴다. 제목·이름·값이 모두 회색이면 위계가 사라진다.

### 12.3 스티키 섹션 탭 (`LabStickySectionNav`)

5개 이상 섹션이 이어지는 긴 페이지에 둔다. 흔한 패턴(쿠팡 상품 상세, 배민 가게 메뉴, 에어비앤비 숙소)과 같은 앵커 탭 + 스크롤 스파이다.

- **등장**: 페이지 상단(제목·탭) 아래 기준점이 사이트 헤더 밑으로 지나가면 페이드 인, 다시 올라오면 사라진다(히스테리시스 4/32px, 120~180ms).
- **구성**: 태블릿 이상은 제목 줄(이름 + 보조) + 탭 줄, **모바일(<640)은 탭 줄만**(49px). 사이트 헤더와 합친 상단 점유를 최소화한다.
- **탭 모양**: 채움 pill. 선택 = 배경 `--lab-teal-50` + 테두리 teal 35% + 글자 `--lab-teal-700` 600, 비선택 = 배경 없음 + 글자 `--lab-muted` 500. 시각 36px, 숨은 영역으로 44px 터치. **밑줄만으로 선택을 표시하지 않는다**(선택감이 약하다는 피드백).
- **동작**: 탭 누르면 섹션 상단이 바 아래 8px에 오도록 스크롤, 위쪽 섹션이 로딩으로 늘어나도 도착 후 재조준. 현재 섹션 탭 자동 강조 + 가로 스크롤 중앙 정렬. 렌더되지 않은 섹션은 탭에서 뺀다.
- **오프셋**: `--lab-sticky-nav-height`를 노출한다. 페이지 안 다른 스크롤 대상은 `scroll-mt-[calc(var(--site-header-height)+var(--lab-sticky-nav-height,0px)+…)]`로 바에 가리지 않게 한다.
- **탭 수**: 가능하면 5개 이하(가로 스크롤 없이 한 줄). 섹션이 더 많으면 짧은 라벨(2~4자)로 가로 스크롤을 허용한다. 각 섹션에는 고정 `id`를 둔다.

### 12.4 목록 (`LabListRow`, `LabMoreButton`)

- **최대 5개 노출** (`LAB_LIST_PREVIEW = 5`), 나머지는 목록 아래 `LabMoreButton` — 라벨 "N곳 더보기"/"N건 더보기"/"N개 동 더보기", 펼치면 "접기". 카드형처럼 큰 항목은 2개까지 줄일 수 있다.
- **예외**: ① 검색 결과처럼 사용자가 조건을 걸어 조회한 표·목록은 페이지 단위(15건) + `Pagination`(시각 36px · 세로 44px 터치, 현재 페이지 `aria-current`)을 쓴다. ② 동·구 선택처럼 고르는 대상 전체를 한눈에 보여야 하는 선택 그리드는 개수 제한 없이 2~4열 타일로 둔다(타일: 이름 15·600 + 보조 13, 44px 이상).
- **무한 스크롤 금지.** 거래 내역 같은 긴 피드도 5건으로 시작해 더보기 클릭마다 다음 묶음(15건)을 붙인다.
- **탭·조건이 바뀌면 펼침 초기화.**
- **행 구조** (`LabListRow`): 이름(15·600 navy, 말줄임) + 보조(13 secondary) 왼쪽 / 값(15·600, 변화는 up/down 색) + 보조 오른쪽 / 이동 가능하면 화살표. 44px 이상, 행 구분선(`LAB_LIST`).
- **근거 태그** (`LabTag`): 순위 이유처럼 짧은 속성은 한 줄 텍스트로 잇지 말고 12px 테두리 태그로 나눈다. 좁으면 태그 단위로 줄바꿈된다(잘림 금지).
- 목록 안에서 동/지역처럼 이름에 붙는 속성은 별도 열 대신 이름 옆 보조 글자로 둔다(열이 이름 폭을 잡아먹지 않게).

### 12.5 지표 박스 (`LabStatTiles`)

| layout | 용도 | 규격 |
|---|---|---|
| `stack` | 2~3개 요약 지표 (시장 온도: 오른 거래 / 신고가 / 고점 −10%) | 테두리 박스 r12, 라벨 위 · 값(15·600) + 보조(13) 아래, **값은 박스 하단 정렬** |
| `inline` | 짧은 변화율 4개 (1개월/1년/3년/최고점) | 라벨 왼쪽 · 값 오른쪽 한 줄, 모바일 2×2 · PC 4열 |

- **라벨은 360px에서 한 줄.** 넘치면 글자를 줄이지 말고 라벨을 줄인다("고점 대비 10%↓" → "고점 −10%"). 정의는 (i)팁으로.
- 3열 박스의 퍼센트는 정수로 표기한다(61.5% → 62%). 원 수치는 필요하면 보조 줄에 건수로.
- 한 줄 요약 지표(예: "2년 전 대비 전세 하락 · 12곳 / 170곳")는 박스 대신 라벨 왼쪽 · 값 오른쪽 한 줄로 쓴다.

### 12.6 강조 칩 · 탭 카운트

- **강조 칩** (`LabEmphasisChip`): 대표값 아래의 순위·등급("서울 25개 구 중 3위", "1년 상승률 14위"). 배경 `--lab-brand-subtle`, 테두리 `--lab-brand-border`, 글자 청록 13·600, 28px(+44px 터치). 누르면 ▾ 회전과 함께 아래에 작은 순위표(현재 항목 앞뒤 5개 + "N개 전체 보기")를 펼친다. 한 번에 하나만 열린다.
- **탭 카운트**: `LabTabs` 항목의 `count`로 라벨 옆에 작은 회색 숫자(12px)를 붙인다("30평대 13"). "(13)"이나 "13곳"을 라벨 문자열에 섞지 않는다. 숫자의 의미는 탭 아래 요약 문장으로 설명한다("15억 이하로 거래된 30평대 단지 13곳 · 전체 53곳").
- **탭 라벨 길이**: secondary 탭은 360px에서 한 줄. 긴 라벨은 줄인다("전세가율 높은 순" → "전세가율 순"), 같은 탭 묶음의 어미를 맞춘다("…순").

### 12.7 구성비 · 방향 막대 (`LabStackedBar`)

- 방향 3색은 `LAB_DIRECTION_SEGMENTS`로 통일: 오름 `--lab-change-up`, 보합·기타 `#CBD5E1`, 내림 `--lab-change-down`. 막대·범례·숫자에 같은 순서(오름 → 보합 → 내림)와 색을 쓴다.
- 전체 막대(h 10) + 아래 3칸 수치(비율 굵게 + 건수). 항목별 막대(h 8)는 같은 스케일(`widthPct`)로 규모를 비교하고 내부는 구성비.
- **막대만으로 정보를 전달하지 않는다.** 항목 행마다 수치 줄을 붙인다("오름 5 · 보합·기타 0 · 내림 3", 숫자에만 방향색). 막대는 `aria-hidden`.
- 타사와 구별되는 표현을 시도하되(발산형 막대 등), 가독성이 떨어지면 표준 누적 막대 + 수치 병기를 우선한다(사용자 결정 2026-09-23).

### 12.8 차트

- 기간 토글은 `LabTabs compact`("최근 3년/전체 기간", "최근 5년/전체 기간"). 데이터가 기본 기간보다 짧으면 토글을 숨긴다.
- 값이 없는 앞 구간(수집 시작 전)은 그리지 않고 첫 데이터 달부터 그린다. 연도 눈금은 기간이 길면 2~3년 간격.
- 축 눈금은 정리된 값(0/50/100%, 10%p 단위 등). 기준선(50% 점선, 선택 월 세로선)을 쓰면 의미를 팁에 적는다.
- 그래프를 눌러야만 보이는 정보를 두지 않는다. 월 선택은 상단 월 스테퍼(◀ ▶)로도 가능해야 하며 KPI·박스·구성이 함께 바뀐다.

### 12.9 문구

- 360px에서 한 줄이 기본. 넘치면 ① 라벨 축약 ② 라벨/값 좌우 분리 ③ 정의를 팁으로 이동 순서로 해결한다. 글자 크기를 줄이지 않는다.
- 금액 변화는 "+6.8억 (거래가 34억)"처럼 변화 뒤 괄호로 기준값을 붙인다.
- 세대·실 단위는 "총 N세대 (오피스텔 N세대, 아파트 N세대)"로 합산 후 괄호 분해, 한 유형만 있으면 괄호에 그 유형만.
- 청록 텍스트 링크(`LabTextLink`)는 보조 이동(계산기, 공고 보기)에만 쓴다.

### 12.10 컴포넌트 카탈로그 (`src/components/ui`)

| 컴포넌트 | 파일 | 요약 |
|---|---|---|
| `LabSection`, `LabSectionHeader`, `LabSubsectionHeader`, `LAB_SECTION_SURFACE`, `LAB_SUBSECTION_RULE` | `LabSection.tsx` | 섹션 카드·제목 줄·하위 제목·하위 구분선 |
| `LabStickySectionNav` | `LabStickySectionNav.tsx` | 스티키 섹션 탭 (sections prop) |
| `LabMoreButton`, `LAB_LIST_PREVIEW` | `LabMoreButton.tsx` | 5개 제한 + 더보기/접기 |
| `LabListRow`, `LAB_LIST`, `LabTextLink` | `LabListRow.tsx` | 목록 행 · 청록 텍스트 링크 |
| `LabStatTiles` | `LabStatTiles.tsx` | 지표 박스 stack/inline |
| `LabStackedBar`, `LAB_DIRECTION_SEGMENTS`, `directionSegments` | `LabStackedBar.tsx` | 구성비 막대 · 방향 색 |
| `LabTag`, `LabEmphasisChip` | `LabTag.tsx` | 근거 태그 · 강조(펼침) 칩 |
| `LabTabs` (`count`) | `LabTabs.tsx` | segmented family (11장) + 탭 카운트 |

페이지 전용 조합(예: `RegionStickyNav`의 섹션 목록)은 페이지 폴더에 두고, 위 컴포넌트 밖에서 같은 역할의 클래스 문자열을 새로 만들지 않는다.

### 12.11 다른 페이지 적용 순서

1. 단지 상세: `DetailSection`/`DetailSubsection` → `LabSection`/`LabSubsectionHeader`로 치환, 데스크톱 전용 밑줄 섹션 탭 → `LabStickySectionNav`(모바일 포함), 목록(거래 내역·주변 공급) 5개 + `LabMoreButton`, 시세 요약 박스 → `LabStatTiles`.
2. 동 단위 지역 페이지: 시장 현황 섹션 컴포넌트를 동 코드로 재사용, 섹션 목록만 교체.
3. 검색·홈·계산기: 결과 목록 `LabListRow` + 5개 규칙, 요약 수치 `LabStatTiles`.

각 단계는 기존 동작을 바꾸지 않는 치환 커밋과 디자인 변경 커밋을 나눈다.

### 12.12 완료 체크리스트

- 360/390/430(+1280) DOM 확인: 페이지 가로 넘침 0, 라벨·탭·박스 텍스트 잘림 0, 360에서 한 줄 규칙.
- 목록 초기 ≤5, 더보기/접기 동작, 탭 전환 시 초기화.
- 스티키 탭: 상단 숨김 → 스크롤 시 표시, 점프 후 섹션 상단이 바 아래 8px, 현재 탭 강조.
- 44px 터치(시각 크기는 작아도 됨), 막대·색 정보의 텍스트 병기, (i)팁에 산식·가중치 없음.
- 같은 지표 중복 노출 없음, 하위 영역 구분선만 존재.
